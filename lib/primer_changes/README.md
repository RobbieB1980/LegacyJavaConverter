# Bundled NeoForge primer_changes stubs

Compact **index** files only for offline GUI installs.
Prefer live station ledgers when present:

`C:\gokuai\Data\NeoForge_Primers\26.2\primer_changes_<source>-to-26.2.md`

Shards (per-version detail) are station-only and are not shipped in the converter package.
