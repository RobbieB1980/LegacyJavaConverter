# Evidence packet (cap ~9,000 characters)

Fill this once per error family. Workers and later turns read **this**, not the whole corpus.

## Task

- Failed output / project:
- Error family (one line):
- Acceptance: destination-Java `gradlew build` → `build/libs/*.jar` (or narrower check):

## Versions

- Detected source:
- Target: 26.2
- Primer ledger path:
- Primer shard opened (one):
- 262r shard id / file (one):

## Sources in play (paths only)

-
-

## Target evidence (≤4 short excerpts)

Each: path + line range + 5–15 lines max.

1.
2.
3.
4.

## Mappings (if any)

- symbol / source_ns / target_ns / result:

## Current compiler errors (trimmed)

```
(paste only the relevant error block, not the full Gradle stack)
```

## Validation command

```powershell
powershell -NoProfile -File C:\gokuai\projects\RB-Legacy-Java-Converter\tools\Build-WithDestinationJava.ps1 -ProjectRoot "<FAILED OUTPUT>"
```

## Result (fill after attempt)

- Changed files:
- Build exit:
- Jar path:
- Next recommended task:
- Confidence:
